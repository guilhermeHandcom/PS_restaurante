<%= form_for(@categoria, :html => {role: "form", :class => 'form-horizontal'}) do |f| %>
  <% if @categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria.errors.count, "error") %> prohibited this categoria from being saved:</h2>

      <ul>
      <% @categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <label for="inputEmail3" class="col-sm-2 control-label">Nome</label>
    <div class="col-sm-4">
      <%= f.text_field :nome, :class => 'form-control' %>
    </div>
  </div>


  <div class="actions">
    <% if controller.action_name == "new" %>
      <%= f.submit "Criar categoria", :class => 'btn btn-default' %>
    <% else %>
      <%= f.submit "Atualizar categoria", :class => 'btn btn-default' %>
    <% end %>
  </div>
<% end %>
