<%= form_for(@mesa, :html => {role: "form", :class => 'form-horizontal'}) do |f| %>
  <% if @mesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mesa.errors.count, "error") %> prohibited this mesa from being saved:</h2>

      <ul>
      <% @mesa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="inputEmail3" class="col-sm-2 control-label">Número</label>
    <div class="col-sm-2">
      <%= f.number_field :numero, :class => 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <label for="inputEmail3" class="col-sm-2 control-label">Capacidade</label>
    <div class="col-sm-2">
      <%= f.number_field :capacidade, :class => 'form-control' %>
    </div>
  </div>

  <div class="actions">
    <% if controller.action_name == "new" %>
      <%= f.submit "Criar mesa", :class => 'btn btn-default' %>
    <% else %>
      <%= f.submit "Atualizar mesa", :class => 'btn btn-default' %>
    <% end %>
  </div>
<% end %>
